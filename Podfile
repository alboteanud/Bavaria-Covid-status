# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Bavaria - Covid state' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Bavaria - Covid state

  target 'Bavaria - Covid stateTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Bavaria - Covid stateUITests' do
    # Pods for testing
  end

pod 'Firebase/Functions'

end
